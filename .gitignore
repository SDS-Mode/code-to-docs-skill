# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Output
output/

# Obsidian workspace state
.obsidian/

# Local-only working dirs — never committed
# (scripts/, tests/, and .claude-plugin/ are intentionally TRACKED — marketplace.json is
#  required for plugin discovery — so they must NOT be listed here.)
research/
docs/
docs-vault/
.firecrawl/
.superpowers/
CLAUDE.md

# Generated vault state — defense in depth (vaults normally live under docs-vault/, already ignored)
_state/
